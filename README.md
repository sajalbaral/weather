# weather

A weather forecast app built with vanilla JavaScript that fetches geocoding and forecast data from Open-Meteo, using a centralized state → render architecture and modular ES modules.

## What did I learn?

- Asynchronous JavaScript with async/await
- Consuming REST APIs (fetch, JSON parsing, error handling)
- State-driven UI updates (no direct DOM mutation in async logic)
- Handling loading / success / error states
- Preventing stale async responses with request IDs
- ES module separation (API, UI, app controller)

## Live Demo: https://sajalbaral.github.io/weather/
